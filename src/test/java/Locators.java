import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

public class Locators {

    /*WebElement getForAuth(){
        return Feature.F01_Login.driver.findElement(By.linkText("Form Authentication"));
    }

    WebElement getUsernameLoc(){
      return Feature.F01_Login.driver.findElement(By.id("username"));

  }

  WebElement  getpassLoc(){
        return Feature.F01_Login.driver.findElement(By.name("password"));
  }

    WebElement btnClick(){
        return  Feature.F01_Login.driver.findElement(By.className("fa-sign-in"));
    }

    WebElement getLogoutLoc(){
        return  Feature.F01_Login.driver.findElement(By.id("flash"));
    }

   WebElement getYoulog(){
        return  Feature.F01_Login.driver.findElement(By.xpath("//a[@class=\"button secondary radius\"]"));
    }
    WebElement getYoulogInvalid(){
        return  Feature.F01_Login.driver.findElement(By.id("flash"));
    }
*/

}
